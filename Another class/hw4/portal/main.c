#include<stdio.h>
#include<stdlib.h>
typedef struct City
{
	
	struct *City next;
}City;
int main(void)
{
	int testcase, city, portal, new_portal;
	scanf("%d", &testcase);
	while(testcase--)
	{
		scanf("%d%d%d", &city, &portal, &new_portal);
		for(int i = 0; i < city; i++)
		{
			
		}
	}
	return 0;
}
